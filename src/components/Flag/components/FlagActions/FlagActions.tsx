import { FC } from 'react';

import type { FlagActionsProps } from './types';

export const FlagActions: FC<FlagActionsProps> = ({ children }) => {
  return children;
};

FlagActions.displayName = 'FlagActions';
